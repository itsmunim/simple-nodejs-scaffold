/**
 * @module {{resource.name}}
 * 
 * @description
 * All the exportable files and functions should be added here.
 * This file exposes the portions of this module. Routes should
 * be exposed by default, apart from routes, anythign else should
 * not be exposed unless needed otherwise.
 */
module.exports = {
  route: require('./route'),
};
